# OpenEvolve TSP Demo —— Python 套件需求
# 安裝:pip install -r requirements.txt
#
# 注意:本地 LLM(qwen2.5-coder)不是 pip 套件,
#       它由 Ollama 管理,請用 `ollama pull qwen2.5-coder:7b` 下載。

openevolve        # 演化框架本體
matplotlib        # 畫 TSP 路線圖(route.png)
